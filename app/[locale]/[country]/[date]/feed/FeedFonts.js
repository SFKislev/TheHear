/**
 * Wrapper for CountryFonts component for feed pages
 */

import CountryFonts from '@/components/CountryFonts';

export default function FeedFonts({ country }) {
    return <CountryFonts country={country} />;
}
